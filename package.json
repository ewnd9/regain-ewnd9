{
  "name": "regain-ewnd9",
  "private": true,
  "scripts": {
    "start": "yarn start:dev",
    "start:dev": "discovery",
    "prebuild": "rm -rf build",
    "build": "discovery-build",
    "build:watch": "parcel build regain/ui/index.ts regain/prepare/index.ts --no-minify",
    "deploy": "yarn build && rm -rf .netlify/build && mv build .netlify && netlify deploy"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ],
  "dependencies": {
    "@babel/parser": "^7.3.1",
    "@discoveryjs/cli": "^1.4.1",
    "@discoveryjs/discovery": "^1.0.0-beta.12",
    "async-directory-tree": "^2.3.1",
    "git-url-parse": "^11.1.2",
    "globby": "^11.0.0",
    "pretty-bytes": "^5.3.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-alpha.3.2",
    "typescript": "^3.7.4"
  }
}
