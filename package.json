{
  "name": "regain-ewnd9",
  "private": true,
  "scripts": {
    "start": "yarn start:dev",
    "start:dev": "discovery",
    "prebuild": "rm -rf build",
    "build": "discovery-build",
    "build:watch": "parcel build regain/ui/index.ts regain/prepare/index.ts",
    "deploy": "yarn build && rm -rf .netlify/build && mv build .netlify && netlify deploy"
  },
  "dependencies": {
    "@babel/parser": "^7.3.1",
    "@discoveryjs/cli": "^1.4.1",
    "@discoveryjs/discovery": "^1.0.0-beta.12",
    "async-directory-tree": "^2.3.1",
    "globby": "^11.0.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-alpha.3.2",
    "typescript": "^3.7.4"
  }
}
